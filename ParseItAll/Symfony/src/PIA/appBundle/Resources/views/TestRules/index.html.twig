{% extends 'base.html.twig' %}

{% block body %}
    Test Rules! {{ job.caption }}
    <table>
        {% for level in job.levels %}
            <tr>
                <td>{{ level.level }}</td>
            </tr>
        {% endfor %}
    </table>    
{% endblock %}

